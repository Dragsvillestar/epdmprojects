doctype html
html
  head
    title Payment Successful
    link(rel="stylesheet", href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css")
    style.
      body { 
        font-family: Arial, sans-serif; 
        background-color: #f8f9fa; 
        text-align: center; 
        padding: 50px; 
      }
      .container { 
        background: #fff; 
        padding: 20px; 
        border-radius: 15px; 
        box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.1); 
        display: inline-block; 
        width: 100%;
        max-width: 500px;
        margin: 0 auto; 
      }
      h2 { 
        color: green; 
      }
      p { 
        color: black; 
      }
      .btn-home {
        display: inline-block;
        margin-top: 20px;
        text-decoration: none;
        background: #007bff;
        padding: 12px 25px;
        border-radius: 30px;
        font-size: 16px;
        font-weight: bold;
        color: white;
        border: none;
        transition: background 0.3s, transform 0.2s;
      }
      .btn-home:hover {
        background: #0056b3;
        transform: scale(1.05);
      }
  body
    div.container
      h2 ✅ Payment Successful!
      p Thank you for your payment. Your subscription is now active.
      p You can now access the projects.
      button.btn-home(onclick="window.location.href='/home'") Go to Home
